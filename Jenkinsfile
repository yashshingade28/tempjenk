pipeline {
    agent any
    
    stages {
        stage('Install Dependencies') {
            steps {
                echo "first step"
            }
        }

        stage('Build') {
            steps {
                echo "second step"
            }
        }
    }
}
